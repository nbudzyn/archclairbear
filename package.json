{
  "name": "archclairbear",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "browser-test": "playwright test",
    "browser-test:quiet": "node src/browser-test/playwright-quiet.mjs",
    "browser-test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.0"
  }
}
